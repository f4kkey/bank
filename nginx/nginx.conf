server {
    listen 80;
    root /var/www/payment;
    location / {
        include fastcgi_params;
        fastcgi_pass bank-php-gateway:9000;
        fastcgi_param SCRIPT_FILENAME $document_root/index.php;
    }
}